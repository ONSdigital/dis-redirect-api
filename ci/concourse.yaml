application:
  repo_name: dis-redirect-api
helm:
  repo_name: dis-helm-legacy-chart
  values_files:
    image_tag_key: dis-redirect-api.image.tag
    sandbox:
      - values/dis-sandbox-internal.yaml
aws:
  ecr:
    application: dis-redirect-api
